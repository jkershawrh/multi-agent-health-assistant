# Claim Provenance Registry -- every factual assertion in the quickstart
# Run `make audit-claims` to see unverified claims
#
# Rules:
#   - Every number shown in the UI MUST have an entry here
#   - Claims with verified: false display with "projected" or "paper reference" label
#   - Speedup claims require minimum_speedup_for_claim threshold met on live hardware
#   - Cost claims use "contributes to" / "estimate" language, never "guaranteed"

claims:
  - id: a2a-teaching-subset
    value: "A2A 0.3-style discovery and JSON-RPC teaching subset"
    file: src/orchestrator.py
    line: null
    source: "Source and contract tests cover agent cards, message/send, and tasks/get"
    source_url: "https://a2a-protocol.org/v0.3.0/specification/"
    verified: true
    verified_date: "2026-08-20"
    notes: >
      This is deliberately labeled as a teaching subset, not an A2A
      conformance implementation. The latest released specification is 1.0.

  - id: multi-agent-workflow
    value: "3-agent cooperative workflow"
    file: src/orchestrator.py
    line: null
    source: "In-process integration test routes a synthetic scenario through all 3 agent APIs"
    source_url: ""
    verified: true
    verified_date: "2026-08-20"
    notes: "Workflow completion is verified by tests/integration/test_workflow_api.py"
